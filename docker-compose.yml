
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: charity
      POSTGRES_PASSWORD: charitypass
      POSTGRES_DB: charitydb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
